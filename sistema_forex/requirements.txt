# Ponte MT5 (cliente RPyC que fala com o terminal MT5 sob Wine no container "mt5").
# pymt5linux é o fork mantido do mt5linux — mesmo protocolo RPyC, sem os pins antigos
# (o mt5linux 0.1.9 fixa numpy==1.21.4 e quebra o build). Exige Python >= 3.13.
pymt5linux>=1.0
rpyc>=5.3

# Web / painel
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
jinja2>=3.1,<4
itsdangerous>=2.1,<3        # assinatura do cookie de sessão
python-multipart>=0.0.9     # formulário de login
bcrypt>=4.1,<5              # hash da senha do painel

# Gráficos offline (sem CDN — padrão Empenho)
plotly>=5.20,<6
